---
# You can define custom variables for each flavor here
accent: "{{mauve}}"
# Normal variables can be defined to be used everywhere
parent: "default"
---

Parent: {{parent}}

{{#each flavors as | flavor flavour | }}
# {{titlecase flavour}}
  - Parent Works Here Too: {{../parent}}
  - Chosen Accent: **{{accent}}**
  - Every Color:
    {{ #each colors as | color colorName | }}
    - **{{colorName}}**
    {{/each}}
{{/each}}
